#A Simple Python Project

in any linux terminal(pkg also can if u use termux):

```bash
apt update && apt upgrade -y && apt install git python -y
```

after that:
```bash
git clone https://github.com/kpin331/ma-project.git
python ~/ma-project/limit.py
```

now you've got interactivable inventory

enjoy:)
